import { defineConfig } from 'vitepress'

// https://vitepress.dev/reference/site-config
export default defineConfig({
  title: "@takuma-ru/rlse",
  description: "All-in-one release flow execution package"
})
