```shell
pnpm add @react-md/app
```

```json filename=src/App.json
{
  "title": "App",
  "menu": "main",
  "package": "app",
  "menuIndex": 0
}
```

---

```tsx
const App = () => {
  return (
    <div>
      <h1>App</h1>
    </div>
  );
};
```

---

```ts
const meta = {
  title: "App",
  menu: "main",
  package: "app",
  menuIndex: 0,
};
```
